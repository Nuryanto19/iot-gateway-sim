# ==============================================================================
# IoT Gateway & Simulator - Environment Configuration
#
# INSTRUCTIONS:
# 1. Copy this file to .env (cp .env.example .env)
# 2. Update the values as needed for your environment.
# 3. Default values are pre-configured for local demonstration.
# ==============================================================================

# --- Infrastructure & Security ---
# Authentication token for InfluxDB and Telegraf integration.
INFLUX_TOKEN=my-super-secret-admin-token-123

# --- Gateway Configuration ---
# Network addresses and ports the Gateway service will listen on.
GATEWAY_TCP_ADDR=:5000
GATEWAY_UDP_ADDR=:5001

# --- Infrastructure Connection (Docker/External Services) ---
# Connection details for the MQTT Broker (Mosquitto).
# Ensure the 'ssl://' prefix is used for mTLS-enabled connections.
MQTT_BROKER_ADDR=ssl://localhost:8883
MQTT_CLIENT_ID=iot-gateway-1

# --- Simulator Configuration ---
# Target Gateway addresses for the Simulator to transmit sensor data.
SIM_TARGET_TCP_ADDR=localhost:5000
SIM_TARGET_UDP_ADDR=localhost:5001
