# ==================================
# IoT Gateway & Simulator - Konfigurasi Lingkungan
#
# Salin file ini ke .env (cp .env.example .env) sebelum menjalankan.
# Nilai default di bawah ini sudah cukup untuk menjalankan demo secara lokal.
# ==================================

# --- Konfigurasi Umum ---
# Token untuk InfluxDB & Telegraf.
INFLUX_TOKEN=my-super-secret-admin-token-123

# --- Konfigurasi Gateway (yang berjalan di host) ---
# Alamat dan port yang akan didengarkan oleh Gateway
GATEWAY_TCP_ADDR=:5000
GATEWAY_UDP_ADDR=:5001

# --- Konfigurasi Koneksi ke Infrastruktur (Docker) ---
# Alamat Broker MQTT yang akan dihubungi oleh Gateway
MQTT_BROKER_ADDR=ssl://localhost:8883
MQTT_CLIENT_ID=iot-gateway-1

# --- Konfigurasi Simulator (yang berjalan di host) ---
# Alamat Gateway yang akan dikirimi data oleh Simulator
SIM_TARGET_TCP_ADDR=localhost:5000
SIM_TARGET_UDP_ADDR=localhost:5001
